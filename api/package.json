{
  "name": "api",
  "version": "1.0.0",
  "description": "API for a book store management",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --env-file=.env --watch server.js"
  },
  "author": "Camilo",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "mongoose": "^8.4.4"
  }
}